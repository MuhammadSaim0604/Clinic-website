version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clinic-app
    environment:
      NODE_ENV: production
      PORT: 5000
      MONGO_URI: mongodb+srv://clinic:xLMo8g3ZeQEF8erc@saimportfolio.oiclj9j.mongodb.net/clinic
    ports:
      - "5000:5000"

    restart: unless-stopped
