## Packages
framer-motion | Page transitions, smooth layout animations, and premium visual depth
lucide-react | Standard icons for medical and admin dashboards
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
- Assumes Wouter is used for routing.
- The `/api/auth/login` and `/api/auth/me` endpoints manage dashboard access.
- Dynamic images are supported via config URLs, but fallbacks to Unsplash are provided for premium empty states.
